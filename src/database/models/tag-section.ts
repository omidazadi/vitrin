export class TagSection {
    public tag: string;
    public section: string;
    public shop: string;

    constructor(tag: string, section: string, shop: string) {
        this.tag = tag;
        this.section = section;
        this.shop = shop;
    }
}
